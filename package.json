{
  "name": "@cloudraker/r2-presign",
  "version": "1.1.0",
  "description": "Presign S3-compatible URLs and mint scoped temporary credentials for Cloudflare R2 — SigV4, zero runtime deps.",
  "keywords": [
    "cloudflare",
    "credentials",
    "presign",
    "r2",
    "s3",
    "sigv4",
    "temporary-credentials"
  ],
  "homepage": "https://github.com/CloudRaker/r2-presign#readme",
  "bugs": {
    "url": "https://github.com/CloudRaker/r2-presign/issues"
  },
  "license": "ISC",
  "author": "CloudRaker",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CloudRaker/r2-presign.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json && tsc -p test/tsconfig.e2e.json",
    "test": "vitest --run --project node --project workers",
    "test:e2e": "vitest --run --project e2e",
    "test:all": "vitest --run",
    "test:coverage": "vitest --run --coverage",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "@types/node": "^26.1.0",
    "@vitest/coverage-v8": "^4.1.10",
    "aws4fetch": "^1.0.20",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.107.0"
  },
  "engines": {
    "node": ">=25"
  },
  "packageManager": "pnpm@11.9.0"
}
