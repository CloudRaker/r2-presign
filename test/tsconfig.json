{
  // Unit test: pure web-standard APIs (fetch/URL/crypto). No Workers-specific types.
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["node"],
    "lib": ["ESNext", "DOM"]
  },
  "include": ["*.test.ts", "../src/**/*"],
  "exclude": ["*.e2e.test.ts"]
}
