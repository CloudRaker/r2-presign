{
  // e2e runs in the Workers pool and imports `cloudflare:test` — needs the
  // pool + Workers ambient types on top of the unit base.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "../worker-configuration.d.ts", "@cloudflare/vitest-pool-workers"]
  },
  "include": ["*.e2e.test.ts", "../src/**/*"],
  "exclude": []
}
